package app

var (
	// Version is set by releases to the semantic version of the binary.
	Version = "0.1.0-dev"
	// Commit is set by releases to the source commit used for the build.
	Commit = "unknown"
	// Date is set by releases to the build timestamp.
	Date = "unknown"
)
